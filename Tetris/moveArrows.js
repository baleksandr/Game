topBtn.addEventListener('click', () => {
    moveActionsItems(topBtn.classList[1])
});

leftBtn.addEventListener('click', () => {
    moveActionsItems(leftBtn.classList[1])
});

bottomBtn.addEventListener('click', () => {
    moveActionsItems(bottomBtn.classList[1])
});

dropDownBtn.addEventListener('click', () => {
    moveActionsItems(dropDownBtn.classList[1])
});

rightBtn.addEventListener('click', () => {
    moveActionsItems(rightBtn.classList[1])
});